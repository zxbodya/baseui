import type { BlockPropsT } from '../block/types';
export declare type AspectRatioBoxPropsT = {
    /** Aspect ratio is width divided by height. */
    readonly aspectRatio?: number;
} & BlockPropsT;
