/// <reference types="react" />
import type { GridPropsT } from './types';
export default function Grid({ align, behavior, children, gridColumns, gridGaps, gridGutters, gridMargins, gridMaxWidth, gridUnit, overrides, }: GridPropsT): JSX.Element;
