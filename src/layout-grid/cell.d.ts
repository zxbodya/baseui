/// <reference types="react" />
import type { CellPropsT } from './types';
export default function Cell({ align, children, gridColumns, gridGaps, gridGutters, gridUnit, order, skip, span, overrides, }: CellPropsT): JSX.Element;
